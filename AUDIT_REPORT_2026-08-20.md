# Audit Report — OpenControl for Pixel Buds Pro 2

**Date:** 2026-08-20
**Auditor:** Claude (AI), acting as Lead QA/Documentation Auditor per the task brief
**Scope:** Phases 0–7 per the task brief. This is a report only — no existing project
file has been modified, per the ground rule (`PROJECT_RULES.md` §5 rule 13).

---

## Executive summary

| Phase | 🔴 Critical | 🟡 Moderate | 🟢 Minor | 💡 Suggestion |
|---|---|---|---|---|
| 0 — Inventory | 0 | 1 | 1 | 0 |
| 1 — Structural/Cross-ref | 0 | 2 | 2 | 0 |
| 2 — Traceability | 0 | 1 | 0 | 0 |
| 3 — Labeling audit | 0 | 3 | 0 | 0 |
| 4 — Rule compliance | 0 | 1 | 0 | 0 |
| 5 — Raw wire re-verification | 0 | 0 | 1 | 1 |
| 6 — Editorial/structural | 0 | 0 | 0 | 4 |
| 7 — Gap analysis | 0 | 0 | 0 | 2 |
| **Total** | **0** | **8** | **4** | **7** |

**Headline result: zero 🔴 Critical findings.** No inconsistency found breaks the evidence
chain or directly contradicts a FACT-labeled claim, and Phase 5's independent raw-wire
re-derivation found **zero factual discrepancies** — every checkable numeric/hex claim in
the documentation reproduced exactly from the raw logs. The 8 🟡 Moderate findings are all
documentation-currency issues (a summary section not updated after the detail section it
summarizes moved on — this pattern recurs three times independently: `PROTOCOL.md` §1,
`PROTOCOL.md` §8, and `ARCHITECTURE.md` §5) plus one mislabeled Test-ID and one
inconsistently-applied documentation-hygiene rule. None require redoing any capture or
retracting any protocol claim. Several duplicate-fix opportunities exist (e.g. one
`.gitignore` fix closes both a Phase 0 and a Phase 1 finding) — see the appendix for a
de-duplicated, prioritized patch list.

Some 20+ additional checks across Phases 1–7 (renumbering completeness, ADR sequencing,
section-reference validity, terminology consistency, scope-guardrail compliance,
hardcoded-string traceability, zero-creativity-rule compliance, reproducibility metadata,
and — critically — the full Phase 5 raw-data re-derivation) came back clean and are recorded
in their respective phase sections as "confirmed-clean" or "checked, no finding," for
transparency about what was actually checked versus assumed.

This report was produced in three passes: the lead pass wrote Phases 0, 1, and part of
Phase 3 directly; two parallel delegated passes independently handled Phases 2/4 (reading
all 11 captures' `FINDINGS.md`/`EVENT-NOTES.md` against the lead pass's established core-doc
context) and Phase 5/7 (re-running `tshark` directly against the raw `*-btsnoop_hci.log`
files); the lead pass then merged both, added the remaining Phase 3 finding it could see
directly, and wrote Phase 6 and this summary.

---

## Phase 0 — Inventory & Scope Completeness

**Method:** `find`/`grep` over the full repo tree (root `.md` files, `captures/`,
`images/`), cross-referenced against every filename token mentioned in any `.md` file, and
against the task's own "Scope of Files to Audit" list.

### [🟡 MODERATE] Scope list omitted six existing, load-bearing files
- **Phase:** 0
- **Files involved:** `REVERSE_ENGINEERING.md`, `SECURITY.md`, `CONTRIBUTING.md`, `SCREENSHOTS_PIXEL_BUDS_APP.md`, `SCREENSHOTS_PIXEL_BUDS_WEB_APP.md`, `WORKSTATION_PREPARATIONS.md`
- **Status of this finding:** 🟢 FACT
- **Description:** The task's "Scope of Files to Audit" list does not name these six files, but all six exist on disk, are all listed in `README.md`'s own documentation table, and are cross-referenced by other in-scope docs (`REVERSE_ENGINEERING.md`'s UUID/Message-Group registers are explicitly named as Phase-3 blockers in `TODO.md`; `SECURITY.md` references `ARCHITECTURE.md` §5; `WORKSTATION_PREPARATIONS.md` is cross-referenced from `README.md`'s bricking disclaimer and from `TESTPLAN_BLUETOOTH_HCI_SNOOP.md`'s `CASE-007`). They were pulled into scope for the remainder of this audit, per the task's own Phase 0 instruction.
- **Evidence:** `ls` of repo root shows all six files present (sizes 1.4KB–7.2KB). `README.md`'s documentation table (lines 90–109) lists all six with a one-line purpose each. `TODO.md` line 27–33 lists `REVERSE_ENGINEERING.md` among docs "not yet given a dedicated cross-consistency pass."
- **Suggested resolution:** No file change needed — this is a scope-definition gap in the *task*, already corrected by including these files in Phases 1–7 below. If this audit process recurs, the scope list should be generated by grepping `README.md`'s documentation table rather than hand-maintained, to avoid the same omission next time (see Phase 6 suggestions).
- **Confidence:** High — directly observed file listing and README table.

### [🟢 MINOR] `.gitignore` does not exist; not referenced as if it did
- **Phase:** 0
- **Files involved:** (repo root)
- **Status of this finding:** 🟢 FACT
- **Description:** The task's Phase 0 checklist asks to confirm whether `.gitignore` exists. It does not (`ls -la` shows no `.gitignore` at repo root; only `.gitattributes` exists, containing Git LFS filters for `captures/**/*.{log,mp4,zip,pcapng}`). This is consistent with the rest of the project, though: `PROJECT_RULES.md` rule 19 says sensitive data is "anonymized or excluded via `.gitignore`," which is a live, if minor, inaccuracy since the file it names doesn't exist — see the Phase 1 finding below.
- **Evidence:** `ls -la` at repo root lists `.gitattributes` (219 bytes, 4 LFS filter lines) but no `.gitignore`.
- **Suggested resolution:** Either add a minimal `.gitignore` (even an empty one, or one excluding common OS/IDE cruft) or amend `PROJECT_RULES.md` rule 19's wording to not imply one exists. Low priority — no evidence any sensitive file has actually been committed unredacted.
- **Confidence:** High.

**Master ID index built** (backbone for Phases 1–4, not repeated per-phase below):
- **`CAP-NNN` IDs referenced anywhere:** CAP-001 through CAP-017, with only CAP-001–007, CAP-010, CAP-015–017 corresponding to real folders (11 folders total); CAP-008/009/011/012/013/014 are `planned` rows in the Capture Index with no folder — confirmed consistent, no orphaned references to a non-existent CAP-NNN folder found outside the Capture Index's own "planned" rows.
- **`ADR-NNN`:** ADR-001 through ADR-009 in `DECISIONS.md`, sequential, no gaps, no reuse (confirmed by header grep). Every ADR reference elsewhere resolves to one of these nine.
- **Test-IDs:** ~70 IDs across `TESTPLAN_BLUETOOTH_HCI_SNOOP.md` §1–§5, prefixed by area (`ANC`, `EQP`, `EQS`, `TOUCH`, `HEAD`, `HOLD`, `AUDIO`, `FW`, `INEAR`, `CASE`, `FIND`, `OBS`, `BATT`, `LOUD`, `ADAPT`, `GATT`, `GFPS`, `CALL`, `PAIR`, `CONV`, `MULTI`, `FWUPD`).
- **`PROTOCOL.md` sections:** §0/§0.1, §1, §2/§2.1/§2.2/§2.2a/§2.3, §3, §4.1–§4.5, §5/§5.1, §6 (Framing/Commands & schemas/Behavior/Resolved), §7, §8.

---

## Phase 1 — Structural & Cross-Reference Integrity

### [🟡 MODERATE] `ARCHITECTURE.md`'s own title was not updated in the project rename
- **Phase:** 1
- **Files involved:** `ARCHITECTURE.md` (line 1)
- **Status of this finding:** 🟢 FACT
- **Description:** The project was renamed to "OpenControl for Pixel Buds Pro 2" on 2026-08-14/15 (per `CHANGELOG.md`'s Phase-1 entry: "renamed the project (trademark)"), and every other core doc's title/opening line uses the new name (`README.md` H1, `AGENTS.md` line 1's "OpenControl for Pixel Buds Pro 2", `PROJECT.md` line 3). `ARCHITECTURE.md`'s own H1, however, still reads `## Pixel Buds Pro 2 Control App — Architecture Blueprint` — the old, pre-rename working title, not the current project name. This is the one title in the whole repo that didn't get updated.
- **Evidence:** `grep -n '^## ' ARCHITECTURE.md` → `1:## Pixel Buds Pro 2 Control App — Architecture Blueprint`. Compare `README.md:1:# OpenControl for Pixel Buds Pro 2`, `AGENTS.md:1:## AI Agent Directives & Guardrails — OpenControl for Pixel Buds Pro 2`, `PROJECT.md:3:**Project name:** OpenControl for Pixel Buds Pro 2 — see README.md.`. A repo-wide grep for `Pixel Buds Pro 2 Control` (the old name) returns exactly one hit, this line.
- **Suggested resolution:** Change `ARCHITECTURE.md` line 1 to `## OpenControl for Pixel Buds Pro 2 — Architecture Blueprint` (or equivalent), matching the pattern `AGENTS.md` already uses. `README.md`/`AGENTS.md`/`PROJECT.md` are the source of truth for the current name.
- **Confidence:** High — single unambiguous string match, no interpretation needed.

### [🟡 MODERATE] `PROTOCOL.md` §8's own changelog table omits multiple dated changes already present in the document body
- **Phase:** 1 (also relevant to Phase 3's staleness checks)
- **Files involved:** `PROTOCOL.md` (§8, lines 983–990, vs. body content dated 2026-08-18)
- **Status of this finding:** 🟢 FACT
- **Description:** `PROTOCOL.md` §8 is explicitly "Changelog of this specification" and its last two rows are dated 2026-08-17. But the document body contains several 2026-08-18-dated promotions that have no corresponding §8 row: §4.2's "Field-to-band mapping — 🟢 FACT, promoted 2026-08-18" (the CAP-015 EQ result), §5.1's "Reconnect, Buds-initiated variant — 🟢 FACT, added 2026-08-18" (CAP-016), §7's error-handling table row "capture-verified 2026-08-18" (CAP-016 case-lid/dock findings), and §6's "Resolved" entry for DLCI 0x08 Group `0x04` Code `0x12` ("resolved 2026-08-18"). None of these four substantive 2026-08-18 changes appear as a row in §8, even though §8 is the document's own authoritative "what changed and when" record and every earlier promotion (2026-08-07, 2026-08-12, 2026-08-17 ×2) does have a row.
- **Evidence:** `grep -n '2026-08-18' PROTOCOL.md` matches lines 418 (§4.2), 434, 439, 704 (§5.1), 845–847 (§6), 886, 898, 903, 932, 937, 941, 947, 954–969 (§6 Resolved), 979 (§7) — none of these line numbers fall within §8 (lines 983–990), whose only dated rows are 2026-08-07, 2026-08-12, and two 2026-08-17 entries.
- **Suggested resolution:** Add 2026-08-18 row(s) to `PROTOCOL.md` §8 summarizing the CAP-015 EQ-mapping promotion, the CAP-016 promotions (§5.1, §7, §6 DLCI-0x08 resolution), matching the existing table's format. `PROTOCOL.md` §8 is the natural source of truth for "what changed" — the body content itself is not in question, only the changelog's completeness.
- **Confidence:** High for the omission itself (direct grep evidence); Medium on severity (this is a self-referential documentation-completeness gap, not a factual error in the protocol knowledge itself, so it doesn't break any evidence chain — rated Moderate rather than Critical for that reason).

### [🟢 MINOR] `PROJECT_RULES.md` rule 19 references `.gitignore` for sensitive-data exclusion, but no `.gitignore` exists
- **Phase:** 1
- **Files involved:** `PROJECT_RULES.md` (line 134–136, rule 19)
- **Status of this finding:** 🟢 FACT
- **Description:** See the Phase 0 finding above — this is the same fact, flagged here as a cross-reference-integrity issue: the rule names a specific file as the mechanism, and that file doesn't exist.
- **Evidence:** `PROJECT_RULES.md:134`: "Sensitive or personal data ... is anonymized or excluded via `.gitignore` before committing." No `.gitignore` at repo root.
- **Suggested resolution:** Same as the Phase 0 finding — add a `.gitignore` or adjust the rule's wording. Not merged as a duplicate finding since Phase 0 and Phase 1 evaluate it from different angles (existence vs. cross-reference validity), but only one fix is needed for both.
- **Confidence:** High.

### [🟢 MINOR] Doubled filename prefix in a `CAP-001-FINDINGS.md` example command
- **Phase:** 1 (also Phase 4's hex & script rule)
- **Files involved:** `captures/CAP-001-2026-08-09_08-51-00_08-52-20-Group_Z/CAP-001-FINDINGS.md` (line 271)
- **Status of this finding:** 🟢 FACT
- **Description:** A worked `tshark` command example reads `tshark -r CAP-CAP-001-btsnoop_hci.log -Y 'btrfcomm.len > 0 and data.data contains "e8:e8"'` — the filename has a doubled `CAP-` prefix (`CAP-CAP-001-...`). The real file is `CAP-001-btsnoop_hci.log`. Copy-pasting this exact command as written would fail with a file-not-found error.
- **Evidence:** `captures/.../CAP-001-FINDINGS.md:271`: "...(`tshark -r CAP-CAP-001-btsnoop_hci.log -Y 'btrfcomm.len > 0 and data.data contains "e8:e8"'`)". Actual file on disk: `CAP-001-btsnoop_hci.log` (confirmed via `find`).
- **Suggested resolution:** Fix the typo to `CAP-001-btsnoop_hci.log` in that one line.
- **Confidence:** High.

### [Checked, no finding] Renumbering completeness (`CAP-005`→`CAP-015`, `CAP-007`→`CAP-016`, `CAP-010` 18:30→`CAP-017`)
- **Phase:** 1
- **Status:** 🟢 FACT — confirmed complete, no regression.
- Repo-wide `grep` for every `CAP-NNN` reference (see Phase 0's master ID index) across `PROTOCOL.md`, `DECISIONS.md`, `ARCHITECTURE.md`, `TESTPLAN_BLUETOOTH_HCI_SNOOP.md`, `TODO.md`, `README.md`, `CAPTURE_BLUETOOTH_HCI_SNOOP.md`'s Capture Index, and every `CAP-NNN-FINDINGS.md`/`EVENT-NOTES.md` shows the renumbering is complete and consistent: all current-state references to the 2026-08-18 EQ session, the 2026-08-18 case/removal re-run, and the 2026-08-16 18:30 GATT session correctly use `CAP-015`/`CAP-016`/`CAP-017` respectively; the only remaining mentions of the old, reused IDs pointing at the *new* sessions are inside `CHANGELOG.md`'s own historical `fix:` entry describing the rename itself, which the task explicitly permits. No stale/wrong-session `CAP-005`/`CAP-007`/`CAP-010` reference was found anywhere else. (Full per-file detail deferred to the delegated Phase 2 pass, which walks the finer-grained self-recommended-follow-through chain; this Phase 1 check is the coarser repo-wide grep sweep.)

### [Checked, no finding] `DECISIONS.md` ADR sequencing, `ARCHITECTURE.md` section-reference validity, project-title consistency elsewhere, terminology consistency for "Message Stream"
- **Phase:** 1
- **Status:** 🟢 FACT — all checked clean.
- ADR-001–009 sequential, no gaps/reuse (header grep). Every `ARCHITECTURE.md §N[.M]` citation found repo-wide (in `TODO.md`, `TESTPLAN_BLUETOOTH_HCI_SNOOP.md`, `SECURITY.md`, `REVERSE_ENGINEERING.md`, `README.md`, `PROTOCOL.md`, `PROJECT.md`, `DECISIONS.md`, and two `CAP-NNN-FINDINGS.md` files) resolves to a section that actually exists in `ARCHITECTURE.md` (§1–§16, plus sub-sections §2.1, §8.1, §9.1 — all confirmed present by header grep). Project title "OpenControl for Pixel Buds Pro 2" is used consistently in `README.md`, `AGENTS.md`, `PROJECT.md` (the only exception is the `ARCHITECTURE.md` title finding above). "Message Stream" terminology is used uniformly (100+ occurrences across `PROTOCOL.md` alone); no instances of the alternative phrasings "message-stream envelope" or "libmaestro framing" were found anywhere in the repo, so the terminology-drift risk flagged in the task brief does not materialize in practice — noted as a non-finding for Phase 6.
- `PROTOCOL_NOTES.md`/`TESTPLAN_EN.md` stale-reference regression check: all remaining mentions of these retired filenames are explicitly historical/retirement-notice context (`CHANGELOG.md`'s own record of the fix, `PROJECT_RULES.md`/`TODO.md`/`PROTOCOL.md`/`CAPTURE_BLUETOOTH_HCI_SNOOP.md` all describing them as "retired 2026-08-15" rather than treating them as live documents) — no regression found.
- `CAP-005-recoding.mp4` / `CAP-006-recoding.mp4` filename-typo check: `CAP-005`'s video file genuinely is misnamed on disk (`CAP-005-recoding.mp4`, missing the "r"), but this is already explicitly and correctly documented as a known, acknowledged "(sic — filename as recorded on disk)" in both `CAP-005-FINDINGS.md` and `CAP-005-EVENT-NOTES.md` — not a silent inconsistency, just an uncorrected filesystem typo. `CAP-006`'s equivalent typo was already found and fixed (`CAP-006-EVENT-NOTES.md` line 76 documents the correction from `CAP-006-recoding.mp4` to `CAP-006-recording.mp4`, and the real on-disk file is indeed named `CAP-006-recording.mp4`). Recorded here as a 💡 Suggestion candidate (rename the `CAP-005` file for consistency) rather than a bug finding, since it's self-documented — see the appendix.

---

## Phase 3 — FACT/HYPOTHESIS/ASSUMPTION Labeling Audit (partial — status-taxonomy finding)

### [🟡 MODERATE] Unified finding: at least three incompatible status-label vocabularies coexist, and one binding rule cites a legend that no longer matches `PROTOCOL.md`
- **Phase:** 3
- **Files involved:** `PROJECT_RULES.md` (§1 rules 1 and 4), `PROTOCOL.md` (§0 legend vs. body usage), `captures/CAP-001-.../CAP-001-FINDINGS.md`, `captures/CAP-017-.../CAP-017-FINDINGS.md`, `DESKRESEARCH_FINDINGS.md` (legend)
- **Status of this finding:** 🟢 FACT (the inconsistency itself — the different documents' texts are quoted directly below, not inferred)
- **Description:** Five distinct points of drift, all stemming from one underlying issue — a fourth status tier (🔴) is in heavy, load-bearing use throughout `PROTOCOL.md` and the capture findings, but was never formally added to any of the documents that define the "official" legend:
  1. `PROJECT_RULES.md` §1 rule 1 defines a strict 3-way taxonomy: FACT / HYPOTHESIS / ASSUMPTION. No fourth tier.
  2. `PROTOCOL.md` §0 declares the same 3-way legend (🟢/🟡/⚪ only) — but the document body uses 🔴 **16 times** (e.g. "🔴 OPEN QUESTION" ×6, "🔴 unconfirmed" ×3, "🔴 unidentified", "🔴 Low", "🔴 open" ×3, "🔴 still open"), none of which trace back to a defined symbol in §0.
  3. `CAP-001-FINDINGS.md` and `CAP-017-FINDINGS.md` (the two files the task brief itself uses as calibration examples elsewhere) each declare their own **4-way** legend at the top of the file, formally adding 🔴 **OPEN QUESTION** ("genuinely unresolved by this capture") as a first-class fourth status — i.e., the capture-level docs already fixed this gap locally, but that fix was never carried back up into `PROTOCOL.md` §0 itself.
  4. `DESKRESEARCH_FINDINGS.md` declares a 3-way legend and explicitly claims it is "consistent with `PROTOCOL.md` §0" — true only if §0's own undocumented 🔴 usage is ignored; as written, `DESKRESEARCH_FINDINGS.md`'s legend is consistent with §0's *stated* legend but not with §0's document's *actual, observed* practice one section-body down.
  5. Separately, and independently of the 🔴 question: `PROJECT_RULES.md` §1 rule 4 says "a claim that starts as 🔴 Unconfirmed or 🟡 Secondary must be re-labeled" — as if quoting `PROTOCOL.md`'s legend directly. But "Secondary" does not exist anywhere in `PROTOCOL.md` as a confidence label — the only occurrence of the word is the unrelated table field name "Secondary sources" in §0's metadata table (`| Secondary sources | Official app/web screenshots ... |`). This reads like a citation of an older, since-rewritten version of `PROTOCOL.md`'s legend that predates the current 🟢/🟡/⚪ (FACT/HYPOTHESIS/ASSUMPTION) wording, never updated when the legend changed.
- **Evidence:**
  - `PROJECT_RULES.md:16-25` (rule 1, 3-way) vs. `PROJECT_RULES.md:34-37` (rule 4: "🔴 Unconfirmed or 🟡 Secondary must be re-labeled once it is confirmed or contradicted").
  - `PROTOCOL.md:22-30` (§0 legend, 3-way: "🟢 FACT ... 🟡 HYPOTHESIS ... ⚪ ASSUMPTION").
  - `grep -o '🔴' PROTOCOL.md | wc -l` → `16`.
  - `grep -n 'Secondary' PROJECT_RULES.md PROTOCOL.md` → only `PROTOCOL.md:40` (`| Secondary sources | ... |`, a table header) and `PROJECT_RULES.md:36` (the rule-4 citation) — no other match anywhere in `PROTOCOL.md`, confirming "🟡 Secondary" as a confidence label does not exist in the current document.
  - `captures/CAP-001-.../CAP-001-FINDINGS.md` and `captures/CAP-017-.../CAP-017-FINDINGS.md` each open with a 4-tier legend including 🔴 OPEN QUESTION (exact wording to be quoted in the merged Phase-3 findings from the delegated pass, which read these files in full).
- **Suggested resolution:** Treat `PROTOCOL.md` §0 as the single source of truth (it's the document every other legend claims consistency with), and formally promote 🔴 to a documented fourth tier there — matching what `CAP-001`/`CAP-017` already do locally and what the body of `PROTOCOL.md` itself already assumes in 16 places. Then: (a) update `PROJECT_RULES.md` rule 1 to describe the resulting 4-way taxonomy (FACT/HYPOTHESIS/ASSUMPTION/OPEN QUESTION) so the binding rule matches the document it governs; (b) fix rule 4's "🟡 Secondary" citation to name an actual current label (most likely 🔴 OPEN QUESTION or 🟡 HYPOTHESIS, depending on what rule 4 was originally trying to describe — "a claim that starts unconfirmed and needs re-labeling once evidence arrives" fits 🔴/⚪→🟢 promotion better than anything called "Secondary"); (c) no change needed to `DESKRESEARCH_FINDINGS.md`'s own legend text, since its "consistent with §0" claim becomes true once §0 itself is fixed.
- **Confidence:** High that all five sub-points are accurately quoted/counted as stated (each is a direct grep/read, not an inference). Medium-High on the recommended resolution being the *right* one (promoting 🔴 formally, rather than e.g. removing all 🔴 usage in favor of stricter 3-way discipline) — that's a maintainer judgment call between two valid directions, not a fact this audit can settle unilaterally; flagged as such.

Corpus-wide check (delegated pass, all 11 `CAP-NNN-FINDINGS.md` read in full): **every
one of the 11 files**, not just `CAP-001`/`CAP-017`, declares the identical 4-line legend
verbatim (`🟢 FACT / 🟡 HYPOTHESIS / ⚪ ASSUMPTION / 🔴 OPEN QUESTION`) — this is unanimous,
consistent, load-bearing practice at the capture level, strengthening the recommendation
above: it is the top-level rules (`PROJECT_RULES.md`, `PROTOCOL.md` §0) that need to catch
up to established capture-level practice, not the other way around.

### [🟡 MODERATE] `ARCHITECTURE.md` §5's DLCI 0x02 description is stale relative to the 2026-08-17 address-instability finding
- **Phase:** 3
- **Files involved:** `ARCHITECTURE.md` §5; `PROTOCOL.md` §2.2a, §6; `DESKRESEARCH_FINDINGS.md` (2026-08-17 entry)
- **Status of this finding:** 🟢 FACT
- **Description:** `DESKRESEARCH_FINDINGS.md`'s 2026-08-17 entry found DLCI 0x02's HDLC Address field is not a small fixed set (`0x0000`/`0xD180`) but includes at least two more pairs (`0x1e80`/`0x2680` Sent, `0xe980` Rcvd), and explicitly states this "is a genuinely new implication for `CodecRouter`/`FrameDecoder` design (`ARCHITECTURE.md` §5): DLCI 0x02's `FrameDecoder` cannot hardcode `0x0000`/`0xD180` as the only valid addresses." This *was* correctly promoted into `PROTOCOL.md` §2.2a's Channel/Message ID row and §6's open-items list (both confirmed present, dated 2026-08-17). It was **not** carried into `ARCHITECTURE.md` §5, which still reads: "Two multiplexed addresses observed (`0x00`, `0xD180`); payload content only partly decoded" — with no mention of the newer addresses or the "do not hardcode a fixed address set" design constraint the deskresearch entry specifically flagged for this file.
- **Evidence:** `DESKRESEARCH_FINDINGS.md` 2026-08-17 entry, "Reading, stated at the appropriate confidence level" paragraph (explicitly names `ARCHITECTURE.md` §5/`CodecRouter`). `ARCHITECTURE.md` §5, DLCI 0x02 block: "Two multiplexed addresses observed (`0x00`, `0xD180`); payload content only partly decoded (device serial + firmware on the Rcvd side) — see `PROTOCOL.md` §2.2a." — no update since.
- **Suggested resolution:** Update `ARCHITECTURE.md` §5's DLCI 0x02 description to note the address field is per-connection-negotiated, not a fixed pair, and that `FrameDecoder` must not hardcode a small address set — mirroring `PROTOCOL.md` §2.2a's current text, which is the source of truth here.
- **Confidence:** High.

**Note on `PROTOCOL.md` §8's changelog gap:** already reported under Phase 1 above (stale
§8 changelog table missing 2026-08-18 rows) — the delegated pass independently rediscovered
the same fact and confirms it; not duplicated here to keep the tally accurate.

### [🟡 MODERATE] `PROTOCOL.md` §1's transport-overview table was never updated after §2.3 resolved the "same channel?" question it poses
- **Phase:** 3
- **Files involved:** `PROTOCOL.md` §1 (line 87), §2.3
- **Status of this finding:** 🟢 FACT
- **Description:** `PROTOCOL.md` §1's "Transports overview" table — the very first substantive table in the document, presumably a reader's first orientation — has a row: "Bluetooth Classic RFCOMM ... `libmaestro` control channel (ANC, EQ, touch/head-gesture config) and, possibly, Fast Pair Message Stream events (see §2) | 🟡 HYPOTHESIS that these two use the *same* RFCOMM channel — see §2 and §6 open questions." This framing — a single RFCOMM channel carrying both `libmaestro` and Message Stream traffic, with the open question being *whether* they're the same channel — is exactly the "original framing" that §2.3 explicitly says was "superseded by the finding above" (2026-08-12): three separate DLCIs coexist (0x04 official Message Stream, 0x02 `libmaestro`-candidate, 0x08 private), so the ANC command is now confirmed to be on DLCI 0x04 specifically, *not* sharing a channel with `libmaestro` at all. §1's table was never revised to reflect this — a reader who stops at §1 (a plausible thing to do, since it's billed as the "overview") gets a materially outdated picture of the transport structure that §2.3, forty pages later, has already corrected.
- **Evidence:** `PROTOCOL.md:87`: "🟡 HYPOTHESIS that these two use the *same* RFCOMM channel — see §2 and §6 open questions." Compare `PROTOCOL.md:224-227` (§2.3): "**Original framing (superseded by the finding above, kept for the record...)**: is `libmaestro`'s ANC/EQ control channel the *same* RFCOMM channel as the Fast Pair Message Stream ... or a *separate* RFCOMM channel...? **Update (2026-08-12):** this is no longer a clean binary choice — three structurally distinct RFCOMM sub-protocols are now evidenced..."
- **Suggested resolution:** Rewrite §1's RFCOMM row to reflect the three-DLCI reality (e.g. "three coexisting DLCIs, each independently framed — see §2.3"), removing the now-superseded "same channel?" framing, or add an explicit forward-pointer/superseded-note the way §2.3 itself does for its own predecessor text.
- **Confidence:** High — direct text comparison, not an inference.

### [💡 Confirmed-clean] ADR-009's lifted block is consistently reflected across referencing docs
- **Phase:** 3
- **Files involved:** `DECISIONS.md` ADR-009, `TODO.md` Phase 3, `CAP-006-FINDINGS.md` §5, `ARCHITECTURE.md` §5
- **Status of this finding:** 🟢 FACT
- **Description:** `DECISIONS.md` ADR-009 records the block-lifted result as a dated, appended "Update (2026-08-15)" entry (not an overwrite of the original decision text) — compliant with rule 9a's own convention for `DECISIONS.md`. `TODO.md` Phase 3 and its "Top priority" section both state ANC's `FrameEncoder`/`FrameDecoder` is "implementation-ready per `AGENTS.md` §6." `ARCHITECTURE.md` §5 does not restate this command-specific detail, but its per-DLCI framing gate (the broader, general gate ADR-009 layers on top of) already lists DLCI 0x04 as "🟢 FACT and implementable" — it does not contradict ADR-009, it is simply silent on the narrower command-level detail, which is acceptable since `DECISIONS.md` is the authoritative source for ADR-tracked decisions.
- **Suggested resolution:** Optionally, `ARCHITECTURE.md` §5 could add a one-line cross-reference to ADR-009 for readers who don't separately check `DECISIONS.md` — nice-to-have, not a defect.
- **Confidence:** High.

---

## Phase 2 — Traceability Chain Validation

### [🟡 MODERATE] Test-ID `PAIR-002` misapplied to ordinary re-pairing in three captures
- **Phase:** 2
- **Files involved:** `TESTPLAN_BLUETOOTH_HCI_SNOOP.md` (§1 `PAIR-002` row, §5), `CAPTURE_BLUETOOTH_HCI_SNOOP.md` §9 (Capture Index rows for `CAP-002`/`CAP-003`/`CAP-004`), `captures/CAP-002-.../CAP-002-EVENT-NOTES.md`, `captures/CAP-003-.../CAP-003-EVENT-NOTES.md`, `captures/CAP-004-.../CAP-004-EVENT-NOTES.md`
- **Status of this finding:** 🟢 FACT — directly verified by reading both sides.
- **Description:** `TESTPLAN_BLUETOOTH_HCI_SNOOP.md` defines `PAIR-002` narrowly as "Pairing / bonding handshake from a **true factory-reset state**," tied specifically to `CASE-007`'s 30-second destructive button hold ("Optional, one-time, destructive comparison capture ... see Group P #16"). Per the Capture Index, `CASE-007`/`PAIR-002` has never actually been executed by any capture — Group P #16 is still listed as optional/not-yet-run. Despite this, `CAP-002-EVENT-NOTES.md`'s "17:05:33 | Tap **Pair**" row, `CAP-003-EVENT-NOTES.md`'s "20:59:42" row, and `CAP-004-EVENT-NOTES.md`'s "06:24:24 | Tap 'Pixel Buds Pro 2 van Ted'" row all tag an ordinary "delete stored link key, forget-and-re-pair" action as `PAIR-002` — none of these three sessions performed a factory reset. The Capture Index propagates the same mistake in its Test(s) columns for `CAP-002`/`CAP-003`. Compare `CAP-010`/`CAP-017`, which perform the same kind of forget-and-re-pair action and correctly tag it only `PAIR-001`.
- **Evidence:** `TESTPLAN_BLUETOOTH_HCI_SNOOP.md` §1: `PAIR-002` row text as quoted above. `CAP-002-EVENT-NOTES.md`: `| 17:05:33 | Tap **Pair** | User (App) | PAIR-002 | Simple Pairing Complete ... |`. `CAP-004-EVENT-NOTES.md`: `| 06:24:24 | Tap "Pixel Buds Pro 2 van Ted" in the list | User (App) | PAIR-002 | Delete Stored Link Key ... |`. `CAPTURE_BLUETOOTH_HCI_SNOOP.md` §9, `CAP-002` row Test(s): `` `PAIR-001`, `PAIR-002`, `CASE-001` ``.
- **Suggested resolution:** Correct the Test-ID tags in `CAP-002-EVENT-NOTES.md`, `CAP-003-EVENT-NOTES.md`, `CAP-004-EVENT-NOTES.md`, and the affected Capture-Index rows from `PAIR-002` to `PAIR-001` (or drop the redundant `PAIR-002` tag, since `PAIR-001` already applies). `TESTPLAN_BLUETOOTH_HCI_SNOOP.md`'s `PAIR-002` definition is unambiguous and consistent with `CAPTURE_BLUETOOTH_HCI_SNOOP.md` Group P's own text — treat it as the source of truth.
- **Confidence:** High — both the Test-ID's definition and the three captures' described procedures are explicit; not a matter of interpretation.

### [💡 Confirmed-clean] Self-recommended follow-throughs — systematically checked, one item legitimately still pending review (not a defect)
- **Phase:** 2
- **Files involved:** all 11 `CAP-NNN-FINDINGS.md` files; `PROTOCOL.md` §4.1/§6; `DECISIONS.md` ADR-009; `CAPTURE_BLUETOOTH_HCI_SNOOP.md` §9; `TESTPLAN_BLUETOOTH_HCI_SNOOP.md` `GATT-001`
- **Status of this finding:** 🟢 FACT
- **Description:** Every "recommend updating X" instruction found across all 11 findings files was traced to its target. All were carried out: `CAP-004-FINDINGS.md` §5's recommended `CAP-002-FINDINGS.md` §3 correction (done); its recommended upgrade of `CAP-002`'s Group `0x07`/Code `0x41` finding to 🟢 FACT (done, "Task 3 (2026-08-12)" addendum); `CAP-006-FINDINGS.md` §5's recommended `DECISIONS.md` update lifting the ADR-009 block (done); `CAP-007-FINDINGS.md`(old) §4's proposed DLCI 0x08 Code `0x12` revision (done, promoted into `PROTOCOL.md` §6's "Resolved" section 2026-08-18); `CAP-017-FINDINGS.md` §5's recommendations (done, already confirmed by the task brief). The one exception — `CAP-016-FINDINGS.md` §8's proposed ANC Notify field-table refinement — is correctly and explicitly left unapplied pending maintainer sign-off, per `AGENTS.md` §6's human-approval requirement for FACT promotions; this is the system working as designed, not a broken follow-through.
- **Suggested resolution:** None needed.
- **Confidence:** High.

### [💡 Confirmed-clean] Historical catalog gaps (`INEAR-004`, `GATT-001`) genuinely closed in `TESTPLAN_BLUETOOTH_HCI_SNOOP.md` §9
- **Phase:** 2
- **Status:** 🟢 FACT — `TESTPLAN_BLUETOOTH_HCI_SNOOP.md` §9's current "Open items" list (`OBS-002`, `FWUPD-001`/`002`, `BATT-005`, `PAIR-003`) contains neither `INEAR-004` nor `GATT-001`; both now have assigned capture scenarios and evidence pointers. No stale reference found.
- **Confidence:** High.

### [💡 Confirmed-clean] No FACT-labeled claim found that overclaims multi-capture confirmation from single-capture evidence
- **Phase:** 2
- **Status:** 🟢 FACT — sampled `PROTOCOL.md` §4.1, §4.3 Option A, §5.1, §7 (the ANC/EQ/GATT/case-removal priority areas) — all FACT citations are accurately scoped to what they actually cite (e.g. §4.1 explicitly says "cross-validated within `CAP-001`", singular and accurate; §7's case-lid row correctly says "2 independent captures" and names both).
- **Confidence:** Medium-high — sampled the priority sections, not exhaustively every FACT line in the document.

---

## Phase 4 — Rule & Guardrail Compliance

### [🟡 MODERATE] Rule 9a ("no accumulating corrections in `CAP-NNN-FINDINGS.md`") — widespread in pre-rule captures, two small residual post-rule instances
- **Phase:** 4
- **Files involved:** `CAP-001-FINDINGS.md`, `CAP-002-FINDINGS.md`, `CAP-003-FINDINGS.md`, `CAP-004-FINDINGS.md`, `CAP-005-FINDINGS.md`; `PROJECT_RULES.md` §3 rule 9a; `CHANGELOG.md`
- **Status of this finding:** 🟢 FACT (pattern presence) / 🟡 HYPOTHESIS (severity judgment)
- **Description:** Surveyed all 11 findings files for the accumulating-blockquote-correction pattern:
  - **`CAP-001-FINDINGS.md`** — confirmed severe (already established): 6+ stacked dated corrections in §2/§5. All but the last (2026-08-15) predate rule 9a's 2026-08-15 introduction.
  - **`CAP-002-FINDINGS.md`** — same pattern, moderate: a "Correction (2026-08-11)" plus five "Task N (2026-08-12)" addenda under §2/§3/§7. Entirely pre-rule.
  - **`CAP-004-FINDINGS.md`** — heaviest volume: §5/§5a stack ~ten "Task N (2026-08-12)" addenda, pre-rule — **except** one §10 line, "**Demoted 2026-08-15**...", dated exactly on the rule's introduction date, same shape rule 9a prohibits.
  - **`CAP-003-FINDINGS.md`** — light version (two "Task N (2026-08-12)" addenda in §4), pre-rule.
  - **`CAP-005-FINDINGS.md`** (authored 2026-08-15, i.e. **after** rule 9a existed) — §1 contains one dated addendum explicitly titled "**Correction to `CAP-005-EVENT-NOTES.md`'s Event Timeline (non-destructive, per `PROJECT_RULES.md` §3a...)**" — the clearest live violation in the corpus: it explicitly invokes the non-destructive-correction convention rule 9a itself says does **not** apply to `CAP-NNN-FINDINGS.md` documents, on a file dated after the rule existed.
  - **`CAP-006`, `CAP-007`(old, rewritten), `CAP-010`, `CAP-015`, `CAP-016`** — clean: each is a single current-state document; where earlier drafts were wrong, corrections are stated in an end-of-file "Corrections vs. the original draft" section, not stacked inline in the main body — a good compliant pattern.
  - **`CAP-017-FINDINGS.md`** — a distinct pattern: §4/§4b/§4c are sequential, dated self-contained sub-investigations, each concluding with its own clean current-state statement, not reversing earlier prose inline. **Judgment call:** this satisfies the spirit of rule 9a — a reader doesn't need to reconstruct a value by overlaying corrections — differing structurally from `CAP-001`'s pattern where later blockquotes directly reverse specific claims made earlier in the same bullet.
- **Suggested resolution:** (1) Add an explicit grandfather clause to rule 9a: findings dated before 2026-08-15 are exempt from retroactive cleanup unless independently revised. (2) `CAP-004-FINDINGS.md` §10's "Demoted 2026-08-15" line and `CAP-005-FINDINGS.md` §1's correction addendum should be rewritten in place (fold the demotion directly into the affected claim's text; history belongs in git/`CHANGELOG.md`) since both post-date the rule. (3) Add one short worked "before/after" example to rule 9a — `CAP-001` as non-compliant, `CAP-017`'s §4/§4b/§4c or `CAP-007`(old)'s end-of-file "Corrections" section as compliant.
- **Confidence:** High for pattern-presence; Medium for the `CAP-017` judgment call (a matter of interpretation, as the task brief itself acknowledged).

### [🟢 MINOR] Hex & script rule (4a) — corpus-wide spot-check finds strong compliance; one broken reproduction command
- **Phase:** 4
- **Status:** 🟢 FACT. Spot-checked every capture's findings file, not just `CAP-001`/`CAP-017`: `CAP-002` through `CAP-017` all consistently pair every decoding claim with an exact command and raw hex (e.g. `CAP-003-FINDINGS.md` §4's Task-5 addendum, `CAP-015-FINDINGS.md` §4's full reproduction script and 56-frame table, `CAP-016-FINDINGS.md`'s per-section `tshark` commands) — no claim found stating a conclusion with neither. The one defect is the same `CAP-001-FINDINGS.md` line 271 filename typo already reported under Phase 1 (not double-counted in the tally).
- **Confidence:** High.

### [💡 Confirmed-clean] Scope guardrails, hardcoded-strings exception, zero-creativity rule, reproducibility metadata
- **Phase:** 4
- **Status:** 🟢 FACT, all four checked clean:
  - **Scope guardrails:** no network/GMS dependency, no BLE-scanning pattern broader than ADR-006, no Google trademark/asset reference, no multi-device design found in `ARCHITECTURE.md`/`PROTOCOL.md`/`REVERSE_ENGINEERING.md` (the last is still an unpopulated template, correctly empty).
  - **Hardcoded-strings exception:** the only literal cited under rule 22 (`"google-pixel-buds-pro-v1"`, `AGENTS.md` §12) is itself traced to `CAP-001`/`CAP-002`/`CAP-004` on-the-wire frame numbers, per the rule's own requirement.
  - **Zero-Creativity rule:** no unflagged guess found anywhere across all 11 findings files — every ambiguous point is explicitly marked 🔴/HYPOTHESIS/"not decoded"/"not confirmed," including `CAP-017-FINDINGS.md` §4c's explicit rejection of handle-inference-by-ordinal-position, citing the rule by name.
  - **Reproducibility metadata:** every Capture Index row has firmware/Android/app-version/capture-method filled in — where a field is genuinely unconfirmed, it's marked "⚪ assumed" or "n/a" with an explanation, never silently blank. `TODO.md`'s existing open item on this already correctly tracks it as ongoing.
- **Confidence:** High to Medium-high (absence claims across a large corpus are inherently harder to fully guarantee, but no counterexample was found on any of the four checks).

---

## Phase 5 — Raw Wire-Data Re-Verification

**Method:** independently re-derived every item below from the raw `*-btsnoop_hci.log`
files via `tshark`/Python — the HDLC-unescape + CRC-32 + protobuf-quintet decode pipeline
was reimplemented from scratch (not copy-pasted from any `FINDINGS.md`), per the task's
explicit instruction not to trust prose summaries for this phase. `tshark` 4.6.7.

**Headline result: zero factual discrepancies found.** Every independently re-derivable
numeric/hex claim checked below reproduced exactly. This project's raw-data claims are
unusually well-verified — no Phase 5 item required a correction to the underlying
documentation. The items below are recorded as verification notes (not defects), except
one genuine 🟢 Minor cosmetic finding and one 💡 documentation-enhancement suggestion.

**Confirmed exact, no discrepancy (details in the delegated pass's evidence, condensed here):**

| # | Claim | Command | Result |
|---|---|---|---|
| 1 | `CAP-001` 3-attempt reconnect (frames 732/737, 775/831, 855) | `frame.number` range + `bthci_evt.code==0x03` filters | Exact match: frame 732→737 (Page Timeout 0x04), 738→775→831 (Disconnect), 832→855 (stable) |
| 2 | `CAP-001` 4× `0x12` Set-ANC frames (2039/2132/2159/2193), 2 taps missing | `btrfcomm.dlci==0x04 and data.data contains 08:12` | Exact match — see nuance below |
| 3 | `CAP-006` exactly 4× `0x12`, one per tap | Same filter, full 3441-frame log | Exact: frames 1393/1627/1731/1862, zero others |
| 4 | `CAP-015` EQ field mapping, ±6.0 clamp, preset quintets | Reimplemented HDLC/CRC-32/float32-quintet decode against all 128 Sent-direction DLCI-0x02 subframes | All 5 preset quintets byte-for-byte identical to `PROTOCOL.md` §4.2's table (e.g. Heavy bass `[5.0,3.0,0,0,0]`); max abs. value across 56 EQ frames = exactly 6.0, never exceeded |
| 5 | `CAP-016` buds-initiated reconnect (frames 1213–1217), ACL disconnect reason `0x13`, 73-frame `0x0044` burst (23 with `0xfea9`), HID Feature Report `a3 00 00` | Frame/timestamp/hex extraction | All exact matches |
| 6 | `CAP-017` 137-match GATT-discovery count, frame 680 truncation (71B/15B), "24 of 26" truncated Responses | `btatt.opcode in {...}` filter, `frame.len`/`frame.cap_len` | 137 exact; frame 680 exact; "24 of 26" exact **once the doc's literal Responses-only filter (`opcode==0x11 or 0x09`) is used** — a broader filter including Requests gives a different count (83/81), so the documented filter is correct but easy to mis-reproduce if not copied exactly |
| 7 | `DESKRESEARCH_FINDINGS.md` 2026-08-17: "0 of 1,231" CRC mismatches; 45-byte EQ envelope unique to `CAP-005`; `0x1e80`/`0x2680`/`0xe980` timing windows | Reimplemented pipeline against `CAP-001/002/003/005/006/007/010` | Per-capture subframe counts (160/400/80/163/175/141/112) sum to exactly 1,231, all CRC-OK; 45-byte shape matches only `CAP-005` frames 1245/1321/1338; `CAP-005` burst 2278–2387 and all three `CAP-007` bursts match the documented frame ranges exactly, and align with `CAP-007-FINDINGS.md`'s independently-documented bud-removal (09:15:38) and idle-resume (09:18:43.428) timestamps |
| 8 | `CAP-002`/`CAP-003`/`CAP-004` total packet-count sanity check | `tshark -r <log> \| wc -l` | Exact match to each `EVENT-NOTES.md`'s stated total (50,468 / 2,863 / 2,921) |

### [🟢 MINOR] `DESKRESEARCH_FINDINGS.md`'s 2026-08-17 timing windows are correct at the frame level but rounded imprecisely in prose
- **Phase:** 5
- **Files involved:** `DESKRESEARCH_FINDINGS.md` (2026-08-17 entry), `PROTOCOL.md` §6
- **Status of this finding:** 🟢 FACT
- **Description:** The documented human-readable timestamps for `CAP-007`'s second and third address-triple bursts are off by ~0.6s and one is vaguely rounded: the entry says "09:15:41.4–09:15:44.4" where the actual re-derived range is 09:15:41.994–09:15:45.052, and "09:19:something" where the actual end is 09:18:56.004. The underlying frame ranges (1489–1604, 2057–2241) are exactly correct — only the prose timestamps are imprecise.
- **Evidence:** Independent re-derivation (Phase 5 item 7 above) — exact frame/timestamp extraction from the raw log via the reimplemented pipeline.
- **Suggested resolution:** Tighten the two approximate timestamps in `DESKRESEARCH_FINDINGS.md`'s 2026-08-17 entry to the precise values. Purely cosmetic — the frame numbers (which are what any reproduction would actually use) are already exact.
- **Confidence:** High.

### [💡 Suggestion] `CAP-001`'s two "missing" ANC taps did produce a Notify frame, even without a matching Set frame — worth noting
- **Phase:** 5
- **Files involved:** `captures/CAP-001-.../CAP-001-btsnoop_hci.log`, `captures/CAP-001-.../CAP-001-FINDINGS.md` §5
- **Status of this finding:** 🟢 FACT (the additional data point) / informational for the interpretation
- **Description:** Widening the search window around the two taps with no `0x12` Set frame (~08:51:32/39) turns up `0x13` Notify frames at 08:51:30.03 (state→Transparency) and 08:51:36.71 (state→Off) — i.e. the Buds *did* report a state change at those moments, just without a corresponding phone-side Set write visible in this capture. This isn't in `PROTOCOL.md`/`CAP-001-FINDINGS.md` §5 today, and it's a genuinely relevant data point for the open "UI-state realization vs. silently-failed tap" question ADR-009 flags — it mildly strengthens the "UI-state realization" reading (the state did change, consistent with a real mode change, just not captured as a Set write) over "the tap silently failed to produce any command at all."
- **Evidence:** `tshark -r CAP-001-btsnoop_hci.log -Y "btrfcomm.dlci==0x04 and frame.time>=\"08:51:25\" and frame.time<=\"08:51:42\""` → `0x13` Notify frames at 08:51:30.03 (`0813000401e8e880`) and 08:51:36.71 (`0813000401e8e820`), with no `0x12` Set frame in the same window.
- **Suggested resolution:** Optionally add a one-line note to `CAP-001-FINDINGS.md` §5 citing these two Notify frames alongside the existing "no `0x12` frame" observation — a low-cost enrichment, not a correction (nothing currently stated is wrong).
- **Confidence:** High on the frame-level fact; the interpretive framing ("strengthens X reading") is offered as a suggestion, not a settled conclusion.

### [Caveat, not a project finding] `CAP-016`'s case-lid-open/close zero-signal claim was not independently re-verified
- **Phase:** 5
- **Status:** the delegated pass ran out of time-budget locating the correct lid-open/close timestamp window in `CAP-016-EVENT-NOTES.md` before the ACL disconnect at 06:33:45.152, and instead checked a post-disconnect window that doesn't actually test the claim. This is a **limitation of this audit's coverage**, not a discrepancy found in the project — the underlying `PROTOCOL.md` §7 claim (2-capture-confirmed, `CAP-007` + `CAP-016`) is not contradicted, simply not independently re-checked here. Flagged transparently per the audit's own evidence discipline rather than silently omitted.

---

## Phase 6 — Editorial, Terminology & Structural Improvements

### Terminology consistency — checked, no drift found
"Message Stream" is used as the sole term for that concept across every document that
mentions it (100+ occurrences in `PROTOCOL.md` alone, consistent usage in `ARCHITECTURE.md`,
`CAPTURE_BLUETOOTH_HCI_SNOOP.md`, `TESTPLAN_BLUETOOTH_HCI_SNOOP.md`, `DESKRESEARCH_FINDINGS.md`,
and every `CAP-NNN-FINDINGS.md`). A repo-wide search for the alternative phrasings the task
brief specifically named as a risk — "message-stream envelope", "libmaestro framing" — found
zero occurrences of either. The terminology-drift risk flagged in the task brief does not
materialize in this repo; no finding needed here.

### This audit substantially closes `TODO.md`'s open cross-consistency review item
`TODO.md`'s Setup section lists `PROJECT_RULES.md`, `PROJECT.md`, `DECISIONS.md`,
`PROTOCOL.md`, `REVERSE_ENGINEERING.md`, `README.md`, `CHANGELOG.md`,
`CAPTURE_BLUETOOTH_HCI_SNOOP.md`, `TESTPLAN_BLUETOOTH_HCI_SNOOP.md` as "not yet given a
dedicated cross-consistency pass." All nine were read in full and cross-checked as part of
this audit (Phases 0–7 above). The genuine ambiguities/vagueness/undocumented choices found
across them are:

- The status-taxonomy inconsistency itself (Phase 3) — the single largest "undocumented
  choice" this pass surfaced: a 4-way legend is unanimous, load-bearing practice at the
  capture level, but was never formally adopted in the two documents (`PROJECT_RULES.md`,
  `PROTOCOL.md` §0) that are supposed to define the project's vocabulary.
- `PROJECT_RULES.md` §1 rule 4's stale "🟡 Secondary" citation (Phase 3) — a small piece of
  textual drift from an earlier, since-rewritten version of `PROTOCOL.md`'s legend.
- `PROTOCOL.md` §1's transport-overview table not updated after §2.3 resolved the question
  it poses (Phase 3, new finding above) — the clearest example found of a document's own
  "summary" section lagging behind its "detail" section after a finding evolved.
- `PROTOCOL.md` §8's changelog table lagging the document body by one full day of edits
  (Phase 1/3) — same underlying pattern (a summary/index section not kept in lockstep with
  the content it summarizes) recurring a third time in the same document, suggesting this
  is a structural risk specific to `PROTOCOL.md`'s size/edit frequency rather than three
  unrelated slips — see the structural suggestion below.
- No other genuine ambiguity, contradiction, or undocumented choice was found in
  `PROJECT.md`, `DECISIONS.md`, `README.md`, or `CHANGELOG.md` — all four read as
  internally consistent and consistent with each other. `REVERSE_ENGINEERING.md` is
  currently an unpopulated template (correctly so — no APK analysis session has been logged
  yet per `TODO.md` Phase 2 being entirely unchecked); it has no content to be ambiguous
  about yet, and should be re-audited once populated.

Recommend closing `TODO.md`'s Setup checklist item for these nine files (check them off, or
replace the item with a pointer to this report), rather than leaving it open — the
dedicated pass it called for has now happened.

### [💡 Suggestion] A single machine-readable ID registry would reduce the class of bug already seen once
`CHANGELOG.md`'s own `fix:` entry records a real incident: three second-attempt capture
sessions reused their first attempt's `CAP-NNN` ID, discovered only after the fact and
requiring a coordinated rename across five documents. `DECISIONS.md`'s ADR numbering and
`TESTPLAN_BLUETOOTH_HCI_SNOOP.md`'s Test-ID numbering carry the identical "never reused,
check the highest existing number first" discipline, enforced today by manual convention
only. A single append-only registry file (even a flat CSV/YAML/JSON — `id,type,date,status`
— committed alongside the docs it indexes) that every new `CAP-NNN`/`ADR-NNN`/Test-ID is
required to be added to before use would make a reused ID a mechanically detectable
conflict (a duplicate key) rather than something that has to be caught by a human noticing
a folder-naming collision after the fact, as happened here.

### [💡 Suggestion] A grep-based CI lint script would catch this audit's own Phase 1 findings automatically, going forward
Every Phase 0/1 finding in this report (`ARCHITECTURE.md`'s stale title, the doubled-prefix
`tshark` command, the `.gitignore` reference to a nonexistent file) is mechanically
detectable by a simple script: (a) extract every backtick-quoted `*.md`/`*.log`/`*.mp4`
filename referenced anywhere and confirm it resolves to a real path; (b) extract every
`CAP-NNN`/`ADR-NNN`/Test-ID token and confirm it resolves to an entry in the relevant index;
(c) grep for the old project name as a regression guard now that the rename is done. None of
this requires an LLM — a ~50-line script run in CI (or as a pre-commit hook) on every PR
touching a `.md` file would have caught three of this report's findings before they were
ever committed, and would catch the same classes of regression automatically in the future.
This is exactly the kind of check this audit performed by hand; automating it is the natural
next step.

### [💡 Suggestion] `PROJECT_RULES.md` §3 rule 9a would benefit from one worked before/after example
Phase 4's finding on rule 9a already recommends this specifically: the rule is currently
being interpreted inconsistently in practice (`CAP-001`'s inline-blockquote stacking vs.
`CAP-007`(old)'s end-of-file "Corrections" section vs. `CAP-017`'s sequential dated
sub-investigations), and a maintainer judgment call is needed on whether the latter two
patterns are both compliant or only one is. A short "compliant" and "non-compliant" example
pair added directly to rule 9a's text would resolve this ambiguity for future sessions
without requiring an audit to re-derive the distinction each time.

### [💡 Suggestion] Consider a stated grandfather clause for pre-2026-08-15 findings, distinct from the worked-example suggestion above
Also from Phase 4: most of the rule-9a "violations" found predate the rule's own
introduction (2026-08-15). An explicit sentence in rule 9a — "findings dated before
2026-08-15 are not retroactively required to be cleaned up unless independently revised" —
would let a future audit (or contributor) distinguish "known, accepted legacy debt" from
"a live, ongoing violation" without having to reconstruct the rule's own history from
`CHANGELOG.md` each time, as this audit had to do.

---

## Phase 7 — Gap Analysis: Unexploited or Undiscovered Findings

**Method:** for each raw log, compared the set of DLCIs/handles/event types actually present
against what each capture's `FINDINGS.md`/`EVENT-NOTES.md` mentions, via `tshark` extraction
cross-referenced against `grep` over the prose.

### [💡 Suggestion] `CAP-002`: DLCI 0x03 and DLCI 0x05 carry real payload traffic never mentioned in `CAP-002-FINDINGS.md`
- **Phase:** 7
- **Files involved:** `captures/CAP-002-.../CAP-002-btsnoop_hci.log`, `captures/CAP-002-.../CAP-002-FINDINGS.md`
- **Status of this finding:** 🟡 HYPOTHESIS (the traffic's existence is a fact; its significance/novelty is unassessed)
- **Description:** `CAP-002-FINDINGS.md` discusses DLCI 0x00/0x02/0x04/0x08/0x09/0x0c but never DLCI 0x03 or 0x05, even though both carry real, non-empty RFCOMM payload traffic in this capture's log.
- **Evidence:**
  ```
  tshark -r CAP-002-btsnoop_hci.log -Y "btrfcomm.dlci==0x03 and btrfcomm.len>0" -T fields -e frame.number | wc -l   → 107
  tshark -r CAP-002-btsnoop_hci.log -Y "btrfcomm.dlci==0x05 and btrfcomm.len>0" -T fields -e frame.number | wc -l   → 35
  grep -c "DLCI 0x03\|DLCI 0x05" CAP-002-FINDINGS.md   → 0
  ```
- **Suggested resolution:** A follow-up deskresearch pass should characterize this traffic — it may turn out to be a direction-bit/response-channel artifact of a DLCI already tracked (a non-finding), but that hasn't been checked and shouldn't be assumed either way, per the project's own zero-creativity discipline.
- **Confidence:** Medium — existence of the traffic is certain (direct tshark count); whether it represents a genuinely new, uncharacterized channel is unassessed.

### [💡 Suggestion] `CAP-006`: DLCI 0x0c (the already-known "0x0c0X cluster") carries 50 frames never mentioned in `CAP-006-FINDINGS.md`
- **Phase:** 7
- **Files involved:** `captures/CAP-006-.../CAP-006-btsnoop_hci.log`, `captures/CAP-006-.../CAP-006-FINDINGS.md`
- **Status of this finding:** 🟢 FACT (the omission) — low-value, since the channel is already characterized elsewhere.
- **Description:** DLCI 0x0c — the channel extensively documented in `CAP-002`/`CAP-004`/`CAP-010` — carries 50 payload frames in `CAP-006`, but `CAP-006-FINDINGS.md` (correctly, an ANC-focused session) never mentions it. Not a defect — this capture's scope was deliberately ANC-only — but the omission isn't explicitly noted as "out of scope for this session" either, so a future reader can't tell "not analyzed" from "nothing there" without independently checking.
- **Evidence:** `tshark -r CAP-006-btsnoop_hci.log -Y "btrfcomm.dlci==0x0c and btrfcomm.len>0" -T fields -e frame.number | wc -l` → 50. `grep -c "0x0c" CAP-006-FINDINGS.md` → 0.
- **Suggested resolution:** Low priority — a one-line note ("DLCI 0x0c traffic present but out of scope for this ANC-focused session") would close the gap cheaply.
- **Confidence:** Medium.

### [Confirmed negative — no action possible] DESKRESEARCH's 0x18/0x1a inner-field correlation is NOT incidentally resolved by `CAP-015`/`CAP-016`
🟢 FACT. Checked whether the two 2026-08-18 captures (post-dating the 2026-08-17 deskresearch
entry) incidentally contain the `0x1e80`/`0x2680`/`0xe980` HDLC addresses whose inner-field
`0x18`/`0x1a` correlation is flagged "not decoded further." They do not — zero matches for
any of the three addresses in either capture's DLCI-0x02 traffic. This open question
genuinely still requires a purpose-built capture bracketing a connection-reopen event, as
`DESKRESEARCH_FINDINGS.md` itself already recommends; no existing data can close it.

### [Confirmed negative — no action possible] `CAP-017` §6's open handle/UUID questions are NOT incidentally touched by `CAP-015`/`CAP-016`
🟢 FACT. `tshark -Y "btatt.handle==0x0f32"` → 0 frames in both captures; `109b862f-…` does not
appear in either. Expected, since neither is a GATT-focused session — confirms `CAP-014`
(the planned snaplen-fix recapture) remains the only path to closing `GATT-001`'s remaining
handle↔UUID gap.

### [Confirmed negative — `TODO.md` is accurate as written] `CAP-008`/`CAP-009` are genuinely not answerable from existing captures
🟢 FACT. No folder exists for `CAP-008`, `CAP-009`, `CAP-011`–`CAP-014` under `captures/`, and
no phone-call or battery-decline-bracket scenario incidentally appears in any of the 10
captured sessions. `TODO.md`'s "genuinely open, not yet captured" characterization is
accurate — a new, dedicated capture is required for both, not redundant with existing data.

---

## Appendix — Prioritized, unapplied patch list

Per the task's ground rule, nothing below has been applied. Each item is independently
approvable. Ordered by a rough cost/benefit prioritization (cheap + real defect first;
structural suggestions last, since they're larger asks).

### Tier 1 — one-line/few-line factual fixes (cheapest, highest confidence)

1. **`ARCHITECTURE.md` line 1**: change `## Pixel Buds Pro 2 Control App — Architecture Blueprint` → `## OpenControl for Pixel Buds Pro 2 — Architecture Blueprint`. *(Phase 1)*
2. **`captures/CAP-001-.../CAP-001-FINDINGS.md` line 271**: fix `tshark -r CAP-CAP-001-btsnoop_hci.log ...` → `tshark -r CAP-001-btsnoop_hci.log ...`. *(Phase 1/4)*
3. **`DESKRESEARCH_FINDINGS.md`'s 2026-08-17 entry**: tighten "09:15:41.4–09:15:44.4" → "09:15:41.994–09:15:45.052" and "09:19:something" → "09:18:56.004", matching the exact re-derived frame timestamps. *(Phase 5)*
4. **Either** add a minimal `.gitignore` at repo root, **or** reword `PROJECT_RULES.md` rule 19 to not reference a specific file that doesn't exist. *(Phase 0/1 — pick one, closes both findings)*

### Tier 2 — small, targeted content updates (real content gap, no new capture needed)

5. **`PROTOCOL.md` §1**'s RFCOMM transport-overview row: rewrite to reflect the three-coexisting-DLCI reality established in §2.3, removing the superseded "🟡 HYPOTHESIS that these two use the *same* RFCOMM channel" framing (or add a superseded-note pointer to §2.3, matching the pattern §2.3 itself already uses for its own predecessor text). *(Phase 3)*
6. **`ARCHITECTURE.md` §5**'s DLCI 0x02 description: update "Two multiplexed addresses observed (`0x00`, `0xD180`)" to reflect the 2026-08-17 finding that the address field is per-connection-negotiated, not fixed, and add the "do not hardcode a small address set" design note `DESKRESEARCH_FINDINGS.md` specifically flagged for this file. *(Phase 3)*
7. **`PROTOCOL.md` §8**: add changelog row(s) for the 2026-08-18 changes already present in the document body (CAP-015 EQ field-mapping/range/preset-quintet promotion to FACT; CAP-016's three promoted FACTs in §5.1/§7; the DLCI 0x08 Code `0x12` resolution in §6). *(Phase 1/3)*
8. **`captures/CAP-002-EVENT-NOTES.md`, `CAP-003-EVENT-NOTES.md`, `CAP-004-EVENT-NOTES.md`**, and the corresponding rows in `CAPTURE_BLUETOOTH_HCI_SNOOP.md` §9: correct the `PAIR-002` Test-ID tag to `PAIR-001` (no factory reset was performed in any of the three sessions; `PAIR-002` is reserved for a from-true-factory-reset pairing per `TESTPLAN_BLUETOOTH_HCI_SNOOP.md`'s own definition, which has never actually been captured). *(Phase 2)*
9. **`PROJECT_RULES.md` §1 rule 4**: replace "a claim that starts as 🔴 Unconfirmed or 🟡 Secondary" with wording that matches `PROTOCOL.md`'s actual current legend (see Tier 3 item 10 below — best done together with the taxonomy fix, since the right replacement text depends on that decision).

### Tier 3 — a maintainer decision is needed first, then a small edit

10. **Status-taxonomy unification**: decide whether 🔴 becomes a formally documented fourth tier (OPEN QUESTION) in `PROJECT_RULES.md` §1 rule 1 and `PROTOCOL.md` §0 — recommended, since this is already unanimous practice in all 11 `CAP-NNN-FINDINGS.md` files and 16 uses in `PROTOCOL.md`'s own body. Once decided, update rule 4's citation (Tier 2 item 9) and `DESKRESEARCH_FINDINGS.md`'s legend (its "consistent with §0" claim becomes literally true once §0 is fixed) to match. *(Phase 3 — the single most impactful fix in this report, since it's currently a maintainer-facing rule that doesn't match either the document it governs or the documents it's supposed to keep consistent.)*
11. **`PROJECT_RULES.md` §3 rule 9a**: add a short worked "before/after" example (recommend: `CAP-001`'s inline-blockquote stack as non-compliant, `CAP-007`(old)'s end-of-file "Corrections" section or `CAP-017`'s §4/§4b/§4c sequential-dated-subsections as compliant) and an explicit pre-2026-08-15 grandfather clause. Then, only for the two post-rule residual instances found (`CAP-004-FINDINGS.md` §10's "Demoted 2026-08-15" line; `CAP-005-FINDINGS.md` §1's correction addendum, which explicitly and incorrectly invokes the `DECISIONS.md`/`PROTOCOL.md`-only non-destructive-correction convention), rewrite the affected text in place per the now-clarified rule. *(Phase 4)*

### Tier 4 — structural improvements (larger investment, not urgent)

12. A single machine-readable ID registry (`CAP-NNN`/`ADR-NNN`/Test-ID, append-only) to make a reused ID a mechanically detectable duplicate-key conflict rather than a human-noticed folder collision after the fact. *(Phase 6)*
13. A ~50-line grep-based CI lint script (or pre-commit hook) checking: every backtick-quoted filename resolves to a real path; every `CAP-NNN`/`ADR-NNN`/Test-ID token resolves to a known entry; the old pre-rename project name doesn't regress. Would have caught 3 of this report's Tier-1 findings automatically. *(Phase 6)*
14. Optional, low-priority polish: rename `captures/CAP-005-.../CAP-005-recoding.mp4` to `CAP-005-recording.mp4` for filename-pattern consistency with every other capture — currently self-documented as a known typo (not silently wrong), so this is cosmetic only. *(Phase 1)*
15. Two candidate undiscovered-content gaps worth a follow-up look, not urgent: `CAP-002`'s undocumented DLCI 0x03 (107 frames)/DLCI 0x05 (35 frames) traffic, and `CAP-006`'s undocumented-but-already-characterized-elsewhere DLCI 0x0c traffic (50 frames, likely fine to explicitly mark out-of-scope rather than investigate). *(Phase 7)*

### Draft `TODO.md` "Known technical debt" additions

If the maintainer prefers to track these as `TODO.md` line items instead of (or in addition
to) working through this report directly, the following are ready to paste into `TODO.md`'s
currently-empty "Known technical debt" section, per `PROJECT_RULES.md` rule 15:

```
## Known technical debt

- [ ] `ARCHITECTURE.md`'s title predates the 2026-08-14/15 project rename — see AUDIT_REPORT_2026-08-20.md Tier 1 #1.
- [ ] `ARCHITECTURE.md` §5's DLCI 0x02 description is stale relative to PROTOCOL.md §2.2a's 2026-08-17 update — see AUDIT_REPORT_2026-08-20.md Tier 2 #6.
- [ ] PROTOCOL.md §1's transport-overview table still poses the "same channel?" question §2.3 already resolved — see AUDIT_REPORT_2026-08-20.md Tier 2 #5.
- [ ] PROTOCOL.md §8's changelog table is missing 2026-08-18 entries — see AUDIT_REPORT_2026-08-20.md Tier 2 #7.
- [ ] Status-taxonomy mismatch: PROJECT_RULES.md/PROTOCOL.md §0 declare 3-way FACT/HYPOTHESIS/ASSUMPTION; every CAP-NNN-FINDINGS.md uses a 4-way legend with 🔴 OPEN QUESTION; needs a maintainer decision — see AUDIT_REPORT_2026-08-20.md Tier 3 #10.
- [ ] PAIR-002 Test-ID mistagged on CAP-002/CAP-003/CAP-004 (no factory reset was performed) — see AUDIT_REPORT_2026-08-20.md Tier 2 #8.
- [ ] PROJECT_RULES.md §3 rule 9a needs a worked example + grandfather clause; CAP-004-FINDINGS.md §10 and CAP-005-FINDINGS.md §1 have small post-rule violations — see AUDIT_REPORT_2026-08-20.md Tier 3 #11.
```

---

*End of report.*
